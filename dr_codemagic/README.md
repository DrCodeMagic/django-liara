
# Django apps getting started

Example of how to deploy the `dr_codemagic` Django project on Liara.

## Deploying

Clone the repository and deploy:

```bash
$ git clone https://github.com/USERNAME/DrCodeMagic  # replace USERNAME with your GitHub username
$ cd dr_codemagic
$ liara deploy
